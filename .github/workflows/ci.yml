name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install
        run: |
          python -m pip install -U pip
          python -m pip install -e . pytest

      - name: Compile
        run: python -m compileall -q phiesta

      - name: Test
        run: pytest

  optional-extras:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install triplets + ML extras
        run: |
          python -m pip install -U pip
          python -m pip install -e ".[triplets,ml]" pytest

      - name: Compile
        run: python -m compileall -q phiesta

      - name: Test with optional extras
        run: pytest
